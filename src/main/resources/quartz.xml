<?xml version="1.0" encoding="UTF-8"?>
<job-scheduling-data
	xmlns="http://www.quartz-scheduler.org/xml/JobSchedulingData"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.quartz-scheduler.org/xml/JobSchedulingData http://www.quartz-scheduler.org/xml/job_scheduling_data_1_8.xsd"
	version="1.8">

	<!--
		See: http://www.quartz-scheduler.org/docs/cookbook/JobInitPlugin.html
	-->

	<pre-processing-commands>
		<delete-jobs-in-group>*</delete-jobs-in-group>  <!-- clear all jobs in scheduler -->
		<delete-triggers-in-group>*</delete-triggers-in-group> <!-- clear all triggers in scheduler -->
	</pre-processing-commands>

	<processing-directives>
		<!--
			if there are any jobs/trigger in scheduler of same name (as in this
			file), overwrite them
		-->
		<overwrite-existing-data>true</overwrite-existing-data>
		<!--
			if there are any jobs/trigger in scheduler of same name (as in this
			file), and over-write is false, ignore them rather then generating an
			error
		-->
		<ignore-duplicates>false</ignore-duplicates>
	</processing-directives>

	<!-- Ok this is where all the jobs are created: -->

	<schedule>
		<job>
			<name>DailyJob</name>
			<job-class>background.jobs.DailyJob</job-class>
		</job>
		<job>
			<name>CRaterJob</name>
			<job-class>background.jobs.CRaterJob</job-class>
			<job-data-map>
				<entry>
					<key>cRater_verification_url</key>
					<value>https://crater-stg.ets.org/crater/scoring/internal/CraterVerifyServlet</value>
				</entry>
				<entry>
					<key>cRater_scoring_url</key>
					<value>https://crater-stg.ets.org/crater/scoring/internal/CraterScoringServlet</value>
				</entry>
				<entry>
					<key>cRater_client_id</key>
					<value>WISETEST</value>
				</entry>
				<entry key="cRaterRequestDao" value-ref="cRaterRequestDao" />
			</job-data-map>
		</job>
		<trigger>
			<cron>
				<name>TriggerEveryDay</name>
				<job-name>DailyJob</job-name>
				<cron-expression>0 0 23 * * ?</cron-expression>
			</cron>
		</trigger>
		<trigger>
			<cron>
				<name>CRaterTrigger</name>
				<job-name>CRaterJob</job-name>
				<cron-expression>0 0 23 * * ?</cron-expression>
			</cron>
		</trigger>
	</schedule>
</job-scheduling-data>